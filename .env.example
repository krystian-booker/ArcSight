# Vision Tools Environment Configuration
# Copy this file to .env and customize for your environment
# Note: .env file should be added to .gitignore to keep secrets safe

# =============================================================================
# Flask Environment Configuration
# =============================================================================

# Application environment: development, production, or testing
# - development: Enables debug mode with detailed error pages
# - production: Optimized and secure (default)
# - testing: Used for automated tests
FLASK_ENV=production

# Alternative: Enable debug mode directly (overrides FLASK_ENV)
# Set to 1 to enable, 0 to disable
# WARNING: Never enable debug in production/competition!
FLASK_DEBUG=0

# =============================================================================
# Server Configuration
# =============================================================================

# Server host address
# Use 0.0.0.0 to listen on all interfaces (default)
# Use 127.0.0.1 for localhost only
HOST=0.0.0.0

# Server port
PORT=8080

# =============================================================================
# Security Configuration
# =============================================================================

# Secret key for session management and CSRF protection
# IMPORTANT: Generate a unique key for production using:
#   python -c "import secrets; print(secrets.token_hex(32))"
# If not set, a random key is generated (sessions won't persist across restarts)
# SECRET_KEY=your-secret-key-here

# =============================================================================
# Database Configuration
# =============================================================================

# Database URL (optional)
# If not set, uses SQLite in user data directory
# Examples:
#   SQLite: sqlite:///path/to/database.db
#   PostgreSQL: postgresql://user:password@localhost/dbname
# DATABASE_URL=sqlite:///vision_tools.db

# =============================================================================
# Development-Specific Settings
# =============================================================================

# For development, you can use:
# FLASK_ENV=development
# This enables:
# - Debug mode (detailed error pages)
# - Auto-reload on code changes (disabled for camera stability)
