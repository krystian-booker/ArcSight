name: vision-tools
channels:
  - conda-forge
dependencies:
  - python=3.11
  - numpy<2            
  - psutil
  - opencv             
  - pip
  - pip:
      - flask
      - harvesters
      - genicam
      - robotpy-apriltag
      - appdirs
      - reportlab
      - depthai
      - flask-sqlalchemy
      - pytest
      - pytest-flask
      - pytest-cov
      - pytest-mock
